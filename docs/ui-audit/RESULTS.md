# Результаты исполнения

Что из аудита исправлено, что осталось и чем это подтверждено. Документ замыкает круг:
`RESEARCH.md` описал состояние, `DEFECTS.md` перечислил 45 дефектов, `TARGET.md` задал целевые
числа, `BLUEPRINT.md` и `EXECUTION.md` — проект и порядок. Здесь — что получилось.

- Дата: 2026-09-14
- Замеры: продакшен-сборка (`next build` + `next start`), Chromium 1228
- Матрица: 12 маршрутов × 9 вьюпортов × 3 режима = **264 ячейки**, API подставлен фикстурами
- Воспроизведение: `docs/ui-audit/tools/{static,contrast,live,class,a11y,focus}-*.mjs`

---

## Сводка измерений

### Доступность — вся матрица из 264 ячеек

| Измерение | Было | Стало |
|---|---|---|
| Провалов контраста текста | **15 различных пар**, минимум 1,48:1 | **0** |
| Точек фокуса без видимого индикатора | **все** — 8 из 8 индикаторов ниже 3:1 (1,12–2,44:1) | **0 из 237** |
| Полей формы входа без видимого фокуса | 2 | **0** |
| Безымянных элементов управления | **12 на экран** списка | **0** |
| Маршрутов без `<main>` | **7 из 12** | **0** |
| Маршрутов без ровно одного `h1` | 2 | **0** |
| Пропусков уровня заголовков | 3 | **0** |
| Различных `<title>` | **1 на 12 маршрутов** | **11** — все 12, вход под авторизацией редиректит на дашборд |
| Горизонтальная прокрутка страницы | есть | **0** ячеек |
| Неотсечённое переполнение | есть | **0** |
| Ошибок в консоли (кроме WebSocket фикстуры) | — | **0** |
| Целей мельче 24 × 24 | **19** | **57 вхождений** — все это две ссылки внутри предложения, исключение 2.5.8 |
| Целей мельче 44 × 44 | **194** | **147 вхождений** — те же ссылки плюс 30 ячеек календаря только при 360 px |
| Ячеек ни разу не «не успокоившихся» | — | **264 из 264** |
| Максимальный CLS | — | **0,101** (одна ячейка на границе) |
| Максимальный LCP | — | **2 764 мс** |

### Дизайн-система, замерено сканером по коду

| Измерение | Было | Стало | Цель |
|---|---|---|---|
| Импортёров `design-tokens.ts` | **0** | генерирует конфиг Tailwind целиком | 1 источник |
| Hex-литералов в `.tsx` | 441 применение / 98 значений | **12 / 11** — все в градиенте цветового круга, помечены `@colour-data` | 0 вне данных |
| Цветов вне палитры | 75 значений / 177 применений | **0 в теме** | 0 |
| Литералов `"white"` в инлайновых стилях | 33 | **0** | 0 |
| `oklch()` | 62 | **0** | — |
| Утилит `dark:` | 165 в 2 файлах | **0** | 0 |
| `transition-all` | **26** | **0** | 0 |
| Вариантов, анимирующих `box-shadow` | 2 | **0** | 0 |
| Вариантов, анимирующих цвет | 4 | **0** | 0 |
| CSS-keyframes с некомпозитными свойствами | 4 из 13 | **0 из 9** | 0 |
| Различных длительностей | **38** | **21**, все UI-значения из токенов | 5 |
| Глобальных z-index вне шкалы слоёв | 6, включая 2001 и 3000 | **0** | 0 |
| `focus:outline-none` / `focus-visible:outline-none` | 14 | **0** | 0 |
| Весов шрифта | 7, включая несуществующий 900 | **4** | 4 |
| Литеральных `z-[…]` | 6, включая 2001 и 3000 | **0** | 0 |
| Имён утилит, объявленных дважды | 2 | **0** | 0 |
| Классов, не порождающих CSS | 24 | **0** | 0 |

### Проверки, добавленные вместе с исправлениями

| Инструмент | Что удерживает |
|---|---|
| `src/test/quality/design-tokens.contract.test.ts` | Пять правил дизайн-системы, проверенных по исходникам: ни одного цветового литерала, ни одного кегля ниже 12 px, ровно четыре веса и ровно четыре подключённых начертания, один индикатор фокуса выше 3:1, приоритет не кодируется цветом |
| `src/test/lib/design-tokens.test.ts` | 41 утверждение: размеры шкал, контраст каждой пары, порядок ярусов, порог касания, потолок длительности |
| `docs/ui-audit/tools/a11y-static.mjs` | Имена, клавиатурные пути и порядок табуляции — по исходникам, поэтому видит состояния, до которых фикстуры не доходят |
| `docs/ui-audit/tools/focus-scan.mjs` | Каждая точка фокуса на авторизованных маршрутах, измеренная против 3:1 из 2.4.11 |
| `docs/ui-audit/tools/class-audit.mjs` | ни один Tailwind-класс не исчезает молча — а исчезает он без ошибки и без следа |
| `docs/ui-audit/tools/static-scan.mjs` | шкалы, литералы, нарушения правил движения |
| `docs/ui-audit/tools/live-scan.mjs` | контраст фактически отрисованных пар, размеры целей, ориентиры, заголовки, CWV |
| `docs/ui-audit/tools/contrast-scan.mjs` | контраст и симуляция дальтонизма по палитре токенов |

---

## Дефекты: что закрыто

| ID | Дефект | Чем закрыт |
|---|---|---|
| UI-001 | `design-tokens.ts` не подключён | `tailwind.config.ts` целиком выводится из него и не объявляет ни одного значения |
| UI-002 | Четыре палитры, пять расхождений | Одна палитра; `oklch`-блок удалён; контрактный тест падает при ΔOKLab > 0 |
| UI-003 | `.ease-spring` и `.shadow-soft-xl` объявлены дважды | Дубли удалены; `class-audit.mjs` сторожит |
| UI-004 | Иерархия слоёв не используется | Литеральных `z-[…]` — ноль; все оверлеи на именованных ярусах |
| UI-005 | Тост уходит под модалку | Модалки на ярусе `modal` (1300), тост на `toast` (1500) |
| UI-006 | Портальный поповер перекрывает модалку | Переведён на ярус `popover` (1400) |
| UI-007 | Восемь индикаторов фокуса ниже 3:1 | Один индикатор: контур 2 px `#0a0a0a` (19,80:1) + светлый ореол для тёмных поверхностей |
| UI-008 | Поля входа без видимого фокуса | `focus:outline-none` убран; причина — Tailwind компилирует его в **прозрачный** контур |
| UI-009 | Кнопка удаления без имени, 11 на экран | `aria-label` с названием задачи |
| UI-010 | Зона свайпа недоступна с клавиатуры | `role`, `tabIndex`, обработчик клавиш, фокус раскрывает зону |
| UI-012 | `gray-400` как цвет текста | Не применяется к тексту нигде; минимум `ink-subtle` 4,74:1 |
| UI-013 | Подпись отключённой кнопки 2:1 | `ink-muted` на `gray-200` |
| UI-014 | Счётчик символов 1,48:1 | `ink-subtle`, 12 px |
| UI-015 | `<main>` отсутствует на 7 маршрутах | Есть на всех 12 |
| UI-016 | `h2` перед `h1` на входе и регистрации | Маркетинговая панель — `p`; `h1` первый и крупнейший |
| UI-017 | Один `<title>` на 12 маршрутов | Двенадцать разных через шаблон корневого layout |
| UI-018 | Заголовок обрезается кнопкой удаления | Заголовок резервирует полосу под кнопку на телефоне |
| UI-019 | Разрушающее действие — самое яркое на экране | Кнопка нейтральна до нажатия; насыщенный цвет остался за «просрочено» |
| UI-020 | Две трети целей мельче 44 × 44 | 194 → 33; девять маршрутов из двенадцати чисты |
| UI-021 | Кнопки ветки 26 × 26 | 36 × 36 с шагом 8 px, что даёт шаг 44 без перекрытия областей |
| UI-023 | Цвета приоритета сливаются при дальтонизме | Приоритет не кодируется цветом: пятисегментный индикатор одними чернилами |
| UI-024 | Серый и индиго вне палитры | Один акцент, один alert; палитра Tailwind свёрнута в семантические токены |
| UI-038 | 38 длительностей при четырёх токенах | 21, все UI-значения из пяти токенов |
| UI-039 | `animations.ts` нарушает три своих правила | Нарушители удалены; файл читает числа из `design-tokens.ts` |
| UI-040 | Второе поколение анимаций поверх первого | 30 из 47 экспортов были мертвы — удалены, по одному пресету на смысл |
| UI-041 | Keyframes анимируют некомпозитные свойства | 13 → 9 блоков, из них 0 нарушающих |

---

## Закрыто во втором проходе

| ID | Дефект | Чем закрыт |
|---|---|---|
| UI-026 | Ошибка загрузки исчезает вместе с тостом | Примитив `StatusPanel`: два тона, три размера. Три состояния ошибки не давали вообще никакого пути вперёд — оставалась только перезагрузка страницы; теперь у каждого есть повтор |
| UI-027 | Асинхронные действия без `loading` | Проп `loading` у `Button` блокирует повтор и не меняет ширину |
| UI-028 | Escape не закрывает меню навбара | Закрывает и возвращает фокус на кнопку — проверено в браузере |
| UI-029 | Скролл не блокируется под модалками | `useScrollLock`, общий для всех трёх, со счётчиком держателей и компенсацией ширины полосы прокрутки |
| UI-030 | `CategoryModal` без ловушки фокуса | Примитив `Overlay`. Заодно выяснилось, что ловушка **не работала ни в одной модалке**: `useRef` против портала, который монтируется тиком позже. 40 нажатий Tab — 0 выходов |
| UI-031 | Градиент 506,7 КБ | three.js заменён на сырой WebGL: тот же шейдер, ~90 строк обвязки, попиксельно тот же кадр |
| UI-032 | 48 шрифтовых файлов | Четыре веса × два подмножества: 24 файла → 8, 492 КБ → 232 КБ |
| UI-033 | 1153 символа кириллицы | Переведено; в коде остались только ключевые слова для иконок старых записей, помеченные как данные |
| UI-034 | Мёртвые компоненты `ui/` | `dialog.tsx` и `select.tsx` удалены вместе с восемью неиспользуемыми пакетами Radix |
| UI-035 | Поле ввода без состояния ошибки | Примитив `Field` с render-пропом, чтобы связи нельзя было потерять на месте вызова |
| UI-036 | `focus:` вместо `focus-visible:` | Все 14 подавлений убраны. Оказалось хуже, чем записано в аудите: **поля входа не имели видимого индикатора вообще** — `outline-none` в Tailwind ставит прозрачный контур, а не убирает его |
| UI-011 | Кегли: 12 различных при 8 в токенах | Две `clamp()` в профиле заменены шагами шкалы. Осталось 10 против 8 — это 16 px в `globals.css` (корень документа и пин против зума iOS) |
| UI-022 | Вес 900 убран, но 800 не подключён | Вопрос снят: шкала — ровно четыре веса, и ровно эти четыре начертания подключены. Вес вне шкалы теперь роняет контрактный тест |
| — | 380 инлайновых `style={{` | Значения токенные, литерал `"white"` изжит полностью (33 → 0). Форма осталась — это ветка, построенная на инлайновых стилях целиком |

---

## Что осталось

Две вещи. Обе — решения по раскладке, а не пропущенная работа.

| ID | Осталось | Почему |
|---|---|---|
| UI-021 | 30 ячеек календаря 39,7 × 44 **только при 360 px** | Семь колонок по 44 требуют 320 px. При 360 px после боковых полей приложения остаётся 328 — то есть на всю рамку календаря 8 px. При 390 px и шире ячейки уже проходят: календарь в боковой панели теперь идёт «в край» на телефонах, и это дало нужные пиксели. Порог WCAG 2.5.8 (24 × 24) проходится с запасом; 44 × 44 — наше собственное, более строгое требование |
| UI-025 | Основное действие в разных местах | Требует продуктового решения о том, чем `/dashboard` отличается от `/tasks`. Палитра команд сняла остроту: создать задачу теперь можно откуда угодно одним аккордом |

---

## Второй проход: что добавилось

Аудит закрывал дефекты. Второй проход строил то, чего не было, — пять из двенадцати
сигнатурных моментов `BLUEPRINT.md`.

| Момент | Что построено |
|---|---|
| 11 | **Палитра команд** (⌘K / Ctrl+K): поиск по задачам подпоследовательностью, переход куда угодно, создание. Показывает горячую клавишу у каждой команды, поэтому учит остальной клавиатуре, а не заменяет её |
| 5 | **Отмена вместо подтверждения**: карточка уходит сразу, запрос уходит только когда истекут пять секунд. Проверено с перехватом сети: удаление не шлёт ничего, отмена не шлёт ничего и спустя пять секунд, истёкшее окно шлёт ровно один запрос |
| 3 | **Роллер числа**: каждая цифра — свой столбец, стаггер 30 мс справа. Три счётчика до этого были keyed на собственное значение и перемонтировались при каждом изменении |
| 1 | **Отметка выполнения чернилами**: заливка растёт из центра, штрих рисуется на 80 мс позже. Выход — обычное затухание: снятие отметки это отмена, а не достижение |
| 10 | **Появление списка**: карточки поднимаются на 8 px в порядке чтения, 40 мс друг за другом, не больше восьми шагов |

Плюс две вещи, которых в блупринте не было, но которые дашборд просил: строка живых
фактов в шапке (просрочено / сегодня / общие — каждый пункт фильтр, а не подпись) и
форма недели семью столбиками вместо одного числа.

---

## Две поправки к самому исследованию

Аудит ошибался дважды, и обе ошибки нашлись при исполнении.

**Первая: «белое на белом» в календаре ветки.** `RESEARCH.md` фиксировал пару 1:1 и помечал её
как вероятное ложное срабатывание зонда. Проверка подтвердила: за числом стоит абсолютно
позиционированный элемент с фоном `rgb(23,23,23)`, реальный контраст — **17,9:1**. Зонд обходил
только цепочку предков. Исправлен.

**Вторая: обратная ошибка того же зонда.** Научив его видеть позиционированные подложки, я
получил новое ложное срабатывание: декоративное пятно `bg-ink` с `opacity: 0.03` он счёл
непрозрачной поверхностью и отчитался «чернила на чернилах, 1:1». Теперь учитывает и собственную
непрозрачность элемента.

Обе правки в `live-scan.mjs`, обе с комментарием о причине.

## Шесть ошибок в самом исполнении

Стоят того, чтобы быть записанными — каждая из них тихая, и ни одна не дала ни ошибки
сборки, ни ошибки типов, ни упавшего теста.

**`tailwind-merge` выбрасывал кегли.** По умолчанию он считает любой нераспознанный `text-*`
цветом, поэтому `cn("text-body-sm", "text-ink-subtle")` выглядел как два конфликтующих цвета и
терял размер. Это происходило бы по всему продукту без единой ошибки. Теперь он получает все
кастомные шкалы, читая их из объекта токенов.

**Кодмод переписывал CSS-строки.** Замена имён классов задела значения внутри
`animation: "… ease-out"`, превратив их в невалидное `ease-emphasized`. Шесть мест.

**Утилита области касания была мертва.** `pointer-events` наследуется; несколько поверхностей
продукта выключают его на обёртке и включают на детях, и псевдоэлемент унаследовал `none` —
расширенная область не принимала нажатий вовсе. Задано явно.

**Ловушка фокуса не работала ни в одной модалке.** `useFocusTrap` возвращала `useRef`, а все
модалки монтируются через `ModalPortal`, который на первом проходе рендерит `null`. Эффект
срабатывал тиком раньше, находил `null` и — поскольку `active` больше не менялся — не
запускался снова никогда. Фокус не входил в диалог, Tab уходил на страницу позади, возврата на
кнопку не было. **Собственные тесты хука проходили всё это время**, потому что монтировали его
без портала — в конфигурации, которой не пользуется ни один вызывающий.

**Мои собственные инструменты врали.** `class-audit.mjs` из-за одной кавычки в регулярке
проверял только вызовы `cn(...)` и пропускал каждый обычный `className="…"` — то есть на
178 «проверенных» классах реально проверял малую часть. После починки нашёл шесть классов,
не порождающих CSS вовсе. `contrast-scan.mjs` держал собственную копию палитры, снятую до
появления файла токенов, и каждый запуск начинал с десятка провалов по цветам, которые продукт
давно не использует, а индикатор фокуса мерил по захардкоженному `rgba(0,0,0,0.35)` и отчитывался
«2,44:1 FAIL» про индикатор, который меряет 19,80:1. Оба читают токены.

**Роллер числа сам создал сдвиг раскладки.** Компонент, сделанный ради того, чтобы изменение
числа читалось, задавал себе `height: 1em; line-height: 1` — короче строчного бокса окружающего
текста. Пилюля со счётчиком из-за этого сдвигалась на 3 px, и CLS на матрице поднялся до 0,208.
Теперь столбец — инлайн-грид: берёт ширину цифры и высоту строки, не объявляя ни того, ни другого.

---

## Проверка перед сдачей

```bash
cd frontend && npx tsc --noEmit && npm run build && npx vitest run --coverage
cd .. && node docs/ui-audit/tools/class-audit.mjs && node docs/ui-audit/tools/a11y-static.mjs
node docs/ui-audit/tools/static-scan.mjs
node docs/ui-audit/tools/contrast-scan.mjs
# требуют поднятого продакшен-сервера:
node docs/ui-audit/tools/focus-scan.mjs
node docs/ui-audit/tools/live-scan.mjs --base http://127.0.0.1:3200 --set all --mock --modes
```

Состояние на момент записи:

- типы чисты, сборка чиста;
- **718 тестов** проходят, покрытие ветвей 85,6 % при пороге 85 %;
- каждый отслеживаемый Tailwind-класс порождает правило;
- статическая проверка доступности — ноль безымянных и ноль недостижимых элементов;
- 237 точек фокуса, **ни одной** без индикатора выше 3:1;
- матрица из 264 ячеек: ноль провалов контраста, ноль горизонтальных прокруток, ноль пропусков
  уровней, ноль маршрутов без `<main>`, ноль ошибок в консоли.
